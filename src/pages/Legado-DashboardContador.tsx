// Deprecated legacy entrypoint: page moved to folder `src/pages/DashboardContador/index.tsx`.
// Keep a thin re-export so existing imports keep working during the refactor.
export { default } from './DashboardContador/index';
